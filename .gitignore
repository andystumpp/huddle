# .NET / WinUI build outputs
# Secrets — don't commit
huddle.env
.env

# Machine-specific provider config (non-secret, but per-machine)
huddle.config.json

# Local runtime data — lives in %LOCALAPPDATA%\Huddle (never in the repo), guard anyway
*.db
*.db-wal
*.db-shm
*.sqlite
scenarios.log

bin/
obj/
*.user
*.suo
Generated Files/

# Tooling caches
.vs/
.idea/

# Publish output
publish/
*.pubxml.user

# OS files
Thumbs.db
.DS_Store
